#clearance.sign-up
  %h2= t('.title')
  = form_for @user do |form|
    = render :partial => '/users/form', :object => form
    .submit-field
      = form.submit "Submit", :class => "btn btn-primary"
      %br/
      = link_to "Already a user?", sign_in_path
